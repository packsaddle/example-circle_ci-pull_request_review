require 'spec_helper'

describe Example::CircleCi::PullRequestReview do
  it 'has a version number' do
    expect(Example::CircleCi::PullRequestReview::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
